specVersion: "v1beta"
serviceId: "mlb-throwbacks"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "mlb-throwbacks-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
